
tasks:
  - name: Start the dev server
    init: yarn install
    command: yarn dev

# List the ports to expose. Learn more https://www.gitpod.io/docs/config-ports/
ports:
  - port: 3000
    onOpen: open-browser

vscode:
  extensions:
    - gamunu.vscode-yarn